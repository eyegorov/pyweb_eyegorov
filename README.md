```gherkin
Выполни последовательно:
```
````cython
1.git clone  # сделать URL на проект;
2 Инициализировать виртуальное окружение;
3 pip install -r requirements.txt # развернуть зависимости;
4. Скопировать template.env и заполнить его (copy template.env .env);
5. runserver.